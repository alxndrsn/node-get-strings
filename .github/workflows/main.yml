name: Node.js CI

on:
  push:
    branches: master
  pull_request:

jobs:
  test:
    needs: test-static
    timeout-minutes: 4
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v6

    - run: yarn install --immutable
    - run: yarn test
